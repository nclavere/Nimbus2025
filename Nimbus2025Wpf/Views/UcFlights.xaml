<UserControl x:Class="Nimbus2025Wpf.Views.UcFlights"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Nimbus2025Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox Text="{Binding Filtre, UpdateSourceTrigger=PropertyChanged}" ToolTip="Rechercher ..." />

        <!-- Entêtes de colonnes -->
        <!--<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,0">
            <TextBlock Text="Compagnie" FontWeight="Bold" Width="120" Margin="10 0 0 0" VerticalAlignment="Center"/>
            <TextBlock Text="Départ" FontWeight="Bold" Width="120" Margin="10 0 0 0" VerticalAlignment="Center"/>
            <TextBlock Text="Arrivée" FontWeight="Bold" Width="120" Margin="10 0 0 0" VerticalAlignment="Center"/>
            <TextBlock Text="Date départ" FontWeight="Bold" Width="120" Margin="10 0 0 0" VerticalAlignment="Center"/>
            <TextBlock Text="Date arrivée" FontWeight="Bold" Width="120" Margin="10 0 0 0" VerticalAlignment="Center"/>
        </StackPanel>-->

        <ListBox Grid.Row="2" ItemsSource="{Binding Flights}" Margin="0 20 0 0" 
                 IsSynchronizedWithCurrentItem="True">
            
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0 10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="8*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Column="0" Grid.Row="0" Text="{Binding CompagnyName}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                        <TextBlock Grid.Column="2" Grid.Row="0" Text="{Binding AirportFrom.Name}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="2" Grid.Row="1" Text="{Binding AirportTo.Name}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Grid.Row="0" Text="{Binding Departure, StringFormat={}{0:dd/MM/yyyy HH:mm}}" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Grid.Row="1" Text="{Binding Arrival, StringFormat={}{0:dd/MM/yyyy HH:mm}}" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

    </Grid>
</UserControl>
