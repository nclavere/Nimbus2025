<UserControl x:Class="DemoWPF.Views.UcBienvenue"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DemoWPF.Views" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Label   Grid.Row="1" Grid.Column="1" Content="Nom"  />
        <TextBox Grid.Row="1" Grid.Column="2" MinWidth="200" 
                 Text="{Binding Nom, UpdateSourceTrigger=PropertyChanged}"/>
        <CheckBox Grid.Row="2" Grid.Column="2" Name="chk"
                  Content="Afficher le texte de bienvenue" 
                  IsChecked="{Binding AfficherTextes, UpdateSourceTrigger=PropertyChanged}"/>

        <!--version 1 : avec la vue modele-->
        <!--<TextBlock Grid.Row="4" Grid.ColumnSpan="4"  Margin="20"
                   Text="{Binding TexteBienvenue}" HorizontalAlignment="Center"
                   Visibility="{Binding AfficherTextes, Converter={StaticResource VisibilityConverter}}"/>-->

        <!--version 2 : avec du binding sur la vue -->
        <TextBlock Grid.Row="4" Grid.ColumnSpan="4"  Margin="20"
           Text="{Binding TexteBienvenue}" HorizontalAlignment="Center"
           Visibility="{Binding IsChecked, ElementName=chk, Converter={StaticResource VisibilityConverter}}"/>

    </Grid>
</UserControl>
